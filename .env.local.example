# Auth
JARVIS_AUTH_TOKEN=<generate-a-random-string>
JARVIS_DAILY_API_LIMIT=50

# Supabase
NEXT_PUBLIC_SUPABASE_URL=<from-supabase-dashboard>
NEXT_PUBLIC_SUPABASE_ANON_KEY=<from-supabase-dashboard>
SUPABASE_SERVICE_ROLE_KEY=<from-supabase-dashboard>

# Claude API (use JARVIS_ANTHROPIC_KEY to avoid conflict with Claude Code env)
ANTHROPIC_API_KEY=<claude-api-key>
JARVIS_ANTHROPIC_KEY=<claude-api-key>

# Notion
NOTION_API_KEY=<notion-integration-token>
NOTION_TASKS_DB_ID=<notion-database-id>
NOTION_FITNESS_DB_ID=<notion-fitness-schedule-database-id>
NOTION_CONTACTS_DB_ID=<notion-contacts-database-id>

# OpenAI (TTS)
OPENAI_API_KEY=<openai-api-key>

# Microsoft OAuth (Azure AD app registration)
MICROSOFT_CLIENT_ID=<azure-app-client-id>
MICROSOFT_CLIENT_SECRET=<azure-app-client-secret>
NEXT_PUBLIC_APP_URL=<your-app-url-for-oauth-callback>

# Google OAuth (Google Cloud Console)
GOOGLE_CLIENT_ID=<google-oauth-client-id>
GOOGLE_CLIENT_SECRET=<google-oauth-client-secret>

# Cron auth (for Railway scheduled tasks)
# Must be >= 32 characters — server refuses to start if shorter
CRON_SECRET=<generate-a-random-string>

# Garmin Connect (unofficial API) — username/password since Garmin has no OAuth.
# Treat these as top-tier secrets; never log them.
GARMIN_EMAIL=<garmin-connect-email>
GARMIN_PASSWORD=<garmin-connect-password>

# Application-layer encryption key for sensitive DB columns (OAuth tokens,
# Garmin raw_json). Generate once with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# Rotating this key invalidates all stored tokens and raw_json — treat as break-glass.
CRYPTO_KEY=<base64-32-byte-key>

# Email triage work accounts (override hardcoded defaults).
WORK_GMAIL_ADDRESS=filman@group.infinid.id
WORK_OUTLOOK_ADDRESS=filman@infinid.id
