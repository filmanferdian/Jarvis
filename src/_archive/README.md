# _archive

Retired v2 components and pages. Excluded from TypeScript build (`tsconfig.json` → `exclude`).

Kept here as reference while Jarvis 3.0 "Atmosphere" migration is in flight. Safe to delete once v3 is stable and nothing below is needed for reference.

- `ArcReactor.tsx` — canvas arc-reactor used as the v2 brand glyph. Replaced by `src/components/Mindmap.tsx`.
- `pages/brand/` — v2 brand showcase page. Replaced by `Jarvis 3.0 Design System.html` (spec doc).
- `pages/style-tile/` — v2 component library showcase. No longer maintained.
- `pages/mood/` — v2 data playground. Dev-only.
